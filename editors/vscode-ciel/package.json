{
  "name": "vscode-ciel",
  "displayName": "Ciel Language Support",
  "description": "VSCode language client for Ciel.",
  "version": "0.1.0",
  "publisher": "ciel-lang",
  "license": "MIT",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:ciel",
    "onLanguage:markdown",
    "onCommand:ciel.restartLanguageServer",
    "onCommand:ciel.showSyntaxTree"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "ciel",
        "aliases": [
          "Ciel",
          "ciel"
        ],
        "extensions": [
          ".ciel"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "configurationDefaults": {
      "editor.semanticTokenColorCustomizations": {
        "rules": {
          "*.mutable:ciel": {
            "fontStyle": "underline"
          }
        }
      },
      "[ciel]": {
        "editor.semanticHighlighting.enabled": true,
        "editor.defaultFormatter": "ciel-lang.vscode-ciel"
      },
      "[markdown]": {
        "editor.semanticHighlighting.enabled": true
      }
    },
    "configuration": {
      "title": "Ciel",
      "properties": {
        "ciel.languageServer.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the Ciel language server on non-Windows hosts."
        },
        "ciel.languageServer.path": {
          "type": "string",
          "default": "",
          "description": "Path to the ciel-lsp executable on non-Windows hosts. If empty, the extension searches the workspace build output and PATH."
        },
        "ciel.formatter.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable document formatting for Ciel source files on non-Windows hosts."
        },
        "ciel.formatter.path": {
          "type": "string",
          "default": "",
          "description": "Path to the cielc executable used for formatting on non-Windows hosts. If empty, the extension searches the workspace build output and PATH."
        },
        "ciel.formatter.extraArgs": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Additional arguments passed to `cielc fmt` before the source file path. --write and --check are ignored by the VS Code formatter."
        }
      }
    },
    "commands": [
      {
        "command": "ciel.restartLanguageServer",
        "title": "Ciel: Restart Language Server"
      },
      {
        "command": "ciel.showSyntaxTree",
        "title": "Ciel: Show Tree-sitter Syntax Tree"
      }
    ]
  },
  "scripts": {
    "build:tree-sitter": "node ./scripts/build-tree-sitter.js",
    "build": "npm run build:tree-sitter && node --check ./src/extension.js",
    "test": "npm run build"
  },
  "dependencies": {
    "markdown-it": "^14.3.0",
    "vscode-languageclient": "^9.0.1",
    "web-tree-sitter": "^0.26.10"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.26.10"
  }
}
