// ciel-test: error
// expect-error: generic constraint not satisfied
// expect-error: does not implement `awaitable_future`

import /std/result;

struct Task<T> {
    T value;
}

async Result<usize, Error> bad(Task<usize> task) {
    return await task;
}

i32 main() {
    return 0;
}
