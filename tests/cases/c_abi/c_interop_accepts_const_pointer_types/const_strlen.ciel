// ciel-test: run
// expect-exit: 3

#c_include "string.h"

unsafe extern "C" {
    usize strlen(*const char s);
}

usize main() {
    []const char text = "abc";
    return unsafe { strlen(text.ptr) };
}
