// ciel-test: compile
// expect-c-contains: size_t strlen(const char *s);

#c_include "string.h"

extern "C" {
    usize strlen(*const char s);
}

usize main() {
    []const char text = "abc";
    return strlen(text.ptr);
}
