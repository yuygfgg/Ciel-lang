// ciel-test: error
// expect-error: captured binding

struct Token {
    void tag;
    i64 value;
}

void make_tag() {
    return;
}

i64 main() {
    Token token = { value: 1 };
    i64 |()| update = || {
        token.tag = make_tag();
        return token.value;
    };
    return update();
}
