// ciel-test: error
// expect-error: `Pair` does not implement `Message`

import /std/lib;

struct Pair {
    i64 value;
    bool enabled;
}

i64 main() {
    Pair pair = { value: 4, enabled: true };
    clone_message(&pair);
    return 0;
}
