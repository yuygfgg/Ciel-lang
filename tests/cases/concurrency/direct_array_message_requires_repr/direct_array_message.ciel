// ciel-test: error
// expect-error: does not implement `Message`

import /std/message;

Result<[3]i64, Error> copy_array([3]i64 values) {
    [3]i64 local = values;
    return clone_message(&local);
}
