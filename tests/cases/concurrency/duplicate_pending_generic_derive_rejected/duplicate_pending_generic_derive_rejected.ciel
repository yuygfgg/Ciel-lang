// ciel-test: error
// expect-error: ambiguous generic impls for interface `clone_message`

import /std/lib;

struct Envelope<T> {
    T item;
}

derive<T: Message> Message<Envelope<T>>;
derive<T: Message> Message<Envelope<T>>;

i64 main() {
    return 0;
}
