// ciel-test: error
// expect-error: forbidden capability `clone_message`

import /std/lib;
import /std/message as message;

struct Envelope<T> {
    T item;
}

derive<T: Message> Message<Envelope<T>>;
unsafe derive<T: Message> message::ThreadLocal<Envelope<T>>;

i64 main() {
    return 0;
}
