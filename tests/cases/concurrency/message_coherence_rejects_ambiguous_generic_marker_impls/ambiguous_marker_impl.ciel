// ciel-test: error
// expect-error: ambiguous generic impls for marker interface `thread_local_marker`

import /std/message;

struct Box<T> {
    T value;
}

unsafe impl<T> thread_local_marker(*const Box<T> value) {
    return true;
}

unsafe impl<U> thread_local_marker(*const Box<U> value) {
    return true;
}

i64 main() {
    return 0;
}
