// ciel-test: error
// expect-error: does not implement `Message`

import /std/message;

unsafe extern "C" {
    type Callback = i32 fn(i32);
}

Result<Callback, Error> copy_callback(Callback callback) {
    Callback local = callback;
    return clone_message(&local);
}
