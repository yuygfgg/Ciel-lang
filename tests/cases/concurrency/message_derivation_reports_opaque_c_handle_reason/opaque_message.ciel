// ciel-test: error
// expect-error: does not implement `Message`

import /std/message;

extern "C" {
    opaque struct NativeHandle;
}

Result<NativeHandle, Error> copy_native(NativeHandle handle) {
    return clone_message(&handle);
}
