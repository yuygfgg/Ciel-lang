// ciel-test: error
// expect-error: does not implement `Message`

import /std/lib;
import /std/os/fd as fd;

i64 main() {
    fd::RawFd raw = fd::stdout();
    fd::RawFd copy = must(clone_message(&raw));
    return unsafe { fd::raw_fd(copy) } as i64;
}
