// ciel-test: error
// expect-error: does not implement `Message`

import /std/lib;

i64 main() {
    Fd fd = stdout();
    Fd copy = must(clone_message(&fd));
    return unsafe { raw_fd(copy) } as i64;
}
