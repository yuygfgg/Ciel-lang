// ciel-test: error
// expect-error: closure conversion requires
// expect-error: implement `thread_local_marker`

import /std/lib;

i64 main() {
    Fd fd = stdout();
    i64 |(i64): ThreadLocal| local = |i64 value| value + (raw_fd(fd) as i64);
    return local(1);
}
