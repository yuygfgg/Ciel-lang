// ciel-test: error
// expect-error: does not implement `Message`

import /std/lib;

struct LocalEnvelope {
    Fd fd;
}

i64 main() {
    LocalEnvelope envelope = { fd: stdout() };
    LocalEnvelope copy = must(clone_message(&envelope));
    return unsafe { raw_fd(copy.fd) } as i64;
}
