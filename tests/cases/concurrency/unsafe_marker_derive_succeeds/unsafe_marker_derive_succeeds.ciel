// ciel-test: run
// expect-exit: 0

import /std/message as message;

struct LocalOnly {
    i64 raw;
}

unsafe derive message::thread_local_marker<LocalOnly>;

i64 main() {
    LocalOnly value = { raw: 1 };
    if (!message::thread_local_marker(&value)) {
        return 1;
    }
    return 0;
}
