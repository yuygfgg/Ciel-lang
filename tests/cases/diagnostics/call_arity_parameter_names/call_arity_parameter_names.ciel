// ciel-test: error
// expect-error: call expects 2 arguments, got 1
// expect-error: parameters: `i64 @count`, `bool enabled`

void take(i64 @count, bool enabled) {
    return;
}

void main() {
    take(1);
}
