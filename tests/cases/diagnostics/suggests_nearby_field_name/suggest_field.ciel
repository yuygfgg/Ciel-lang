// ciel-test: error
// expect-error: unknown field `vlaue` on `Packet`
// expect-error: did you mean `value`?

struct Packet {
    i64 value;
}

i64 main() {
    Packet packet = { value: 1 };
    return packet.vlaue;
}
