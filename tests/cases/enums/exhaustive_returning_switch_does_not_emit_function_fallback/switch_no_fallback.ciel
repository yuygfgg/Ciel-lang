// ciel-test: compile
// expect-c-not-contains: ciel_panic(NULL, 0);

enum Choice {
    A,
    B,
}

i64 pick(Choice choice) {
    switch (choice) {
        case A:
            return 1;
        case B:
            return 2;
    }
}

i64 main() {
    return pick(A);
}
