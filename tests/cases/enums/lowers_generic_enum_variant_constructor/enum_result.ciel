// ciel-test: compile
// expect-c-contains: typedef struct Result__def
// expect-c-contains: .as.Ok = { ._0 = 7 }

enum Error {
    Failed,
}

enum Result<T, E> {
    Ok(T),
    Err(E),
}

Result<i64, Error> good() {
    return Ok(7);
}

i64 main() {
    good();
    return 0;
}
