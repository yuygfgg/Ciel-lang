// ciel-test: error
// expect-error: must return

enum Choice {
    A,
    B,
}

i64 pick(Choice choice) {
    switch (choice) {
        case A:
            return 1;
        case B:
            break;
    }
}

i64 main() {
    return pick(A);
}
