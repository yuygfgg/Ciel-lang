// ciel-test: run
// expect-exit: 42

enum Next<Item> {
    Item(Item),
    Done,
}

interface<I -> Item> Next<Item> next(*I iter);
interface Iterator<Item> = next<Item>;

struct Range {
    i64 value;
}

impl next<i64>(*Range iter) {
    return Item(iter->value);
}

_: Iterator<i64> make_range(i64 value) {
    Range range = { value: value };
    return range;
}

i64 main() {
    _ iter = make_range(42);
    switch (next(&iter)) {
    case Item(value):
        return value;
    case Done:
        return 0;
    }
}
