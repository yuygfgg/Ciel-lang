// ciel-test: run
// expect-exit: 1

import ./left_variant;
import ./right_variant;

i64 pick(LeftChoice value) {
    switch (value) {
        case Same:
            return 1;
    }
}

i64 main() {
    LeftChoice value = Same;
    return pick(value);
}
