// ciel-test: error
// expect-error: ambiguous type lookup `Item`
// expect-error: candidates: `Item` (struct)

import ./left_item;
import ./right_item;

i64 main() {
    Item item = { value: 1 };
    return item.value;
}
