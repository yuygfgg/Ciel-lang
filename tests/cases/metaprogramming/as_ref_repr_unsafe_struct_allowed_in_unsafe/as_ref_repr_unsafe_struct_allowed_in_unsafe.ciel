// ciel-test: run
// expect-exit: 0

import /std/meta as meta;

unsafe struct Secret {
    i64 token;
}

i64 main() {
    Secret value = unsafe { { token: 7 } };
    meta::RefRepr<Secret> repr = unsafe { meta::as_ref_repr(&value) };
    return *repr.head.value - 7;
}
