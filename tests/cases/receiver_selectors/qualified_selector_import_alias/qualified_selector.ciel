// ciel-test: run
// expect-exit: 0

import ./selector_symbols as symbols;

i64 main() {
    symbols::Counter @counter = { value: 3 };
    counter.symbols::add(4);
    if (counter.symbols::get() != 7) {
        return 1;
    }
    return 0;
}
