// ciel-test: run
// expect-exit: 0
// expect-stdout: 3

import /std/lib;

i32 main() {
    AsyncSleep op = must(sleep_ms_async(0));
    must(cancel_sleep(op));
    must(print_value(3 as i64));
    return 0;
}
