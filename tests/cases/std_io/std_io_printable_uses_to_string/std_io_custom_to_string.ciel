// ciel-test: run
// expect-exit: 0
// expect-stdout: custom-value

import /std/io;

struct Label {
    []const char text;
}

impl to_string(*const Label value) {
    return value->text;
}

i32 main() {
    Label label = { text: "custom-value" };
    must(print("{}", [label]));
    return 0;
}
