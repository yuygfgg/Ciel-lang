// ciel-test: run
// expect-exit: 0
// expect-stdout: value=42 ok=true signed=-7 byte=255

import /std/io;

i32 main() {
    must(print("value={} ok={}", [42 as usize, true]));
    must(print(" signed={} byte={}", [-7, 255 as u8]));
    return 0;
}
