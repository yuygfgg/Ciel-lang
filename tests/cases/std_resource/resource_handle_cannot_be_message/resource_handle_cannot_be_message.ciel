// ciel-test: error
// expect-error: resource type `File` cannot implement `clone_message`

import /std/io as io;
import /std/message;

unsafe impl clone_message(*const io::File value) {
    return Ok(*value);
}

i32 main() {
    return 0;
}
