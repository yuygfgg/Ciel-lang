// ciel-test: run
// expect-exit: 0

import /std/lib;

i64 main() {
    u64 start = must(monotonic_ms());
    must(sleep_ms(1));
    u64 end = must(monotonic_ms());
    if (end < start) {
        return 1;
    }
    return 0;
}
