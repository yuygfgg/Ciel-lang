// ciel-test: error
// expect-error: closure conversion requires
// expect-error: implement `clone_message`
// expect-error: raw pointer

import /std/lib;

i64 main() {
    i64 local = 1;
    *i64 ptr = &local;
    i64 |(i64): Message| handler = |i64 value| value + *ptr;
    return handler(0);
}
