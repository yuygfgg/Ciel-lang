// ciel-test: run
// expect-exit: 7

i64 apply(i64 |(i64)| func, i64 value) {
    return func(value);
}

i64 main() {
    i64 base = 3;
    i64 |(i64)| add_base = |x| x + base;
    base = 100;
    return apply(add_base, 4);
}
