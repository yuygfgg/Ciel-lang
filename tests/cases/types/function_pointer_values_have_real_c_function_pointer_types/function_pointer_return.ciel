// ciel-test: run
// expect-exit: 7

i64 add_one(i64 value) {
    return value + 1;
}

i64 fn(i64) choose() {
    return add_one;
}

i64 main() {
    i64 fn(i64) local = choose();
    return local(6);
}
