// ciel-test: error
// expect-error: nullable pointer

i64 main() {
    i64 value = 0;
    *i64 ptr = &value;
    ?*void erased = ptr as ?*void;
    *i64 restored = erased as *i64;
    return *restored;
}
