// ciel-test: error
// expect-error: `null` requires an expected nullable pointer type
// expect-error: cannot infer type hole in local `ptr`
// expect-error: array literal requires an expected array or slice type
// expect-error: struct literal requires an expected struct type
// expect-error: closure parameter `value` requires an explicit type or expected callable type
// expect-error: block-bodied closure requires an expected callable return type
// expect-error: cannot infer type hole in local `nullable`

i64 main() {
    _ ptr = null;
    ?*_ nullable = null;
    _ empty = [];
    _ point = { x: 1, y: 2 };
    _ inc = |value| value + 1;
    _ block = |i64 value| {
        return value + 1;
    };
    return 0;
}
