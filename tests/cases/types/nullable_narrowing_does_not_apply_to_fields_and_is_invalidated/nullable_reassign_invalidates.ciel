// ciel-test: error
// expect-error: requires non-null pointer

struct Box {
    i64 value;
}

i64 main() {
    Box box = { value: 1 };
    ?*Box ptr = &box;
    if (ptr != null) {
        ptr = null;
        return ptr->value;
    }
    return 0;
}
