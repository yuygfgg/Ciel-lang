// ciel-test: error
// expect-error: recursive by-value type is not supported: `List`
// expect-error: layout cycle: List -> List

enum List {
    Cons(i64, List),
    Nil,
}

i64 main() {
    return 0;
}
