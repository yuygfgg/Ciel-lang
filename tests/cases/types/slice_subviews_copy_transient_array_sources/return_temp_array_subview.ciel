// ciel-test: run
// sanitizer: address
// expect-c-contains: ciel_alloc_atomic_array(sizeof(int64_t), 2)
// expect-exit: 8

[]const i64 make() {
    return ([7, 8] as [2]i64)[..];
}

usize main() {
    []const i64 values = make();
    return values[1] as usize;
}
