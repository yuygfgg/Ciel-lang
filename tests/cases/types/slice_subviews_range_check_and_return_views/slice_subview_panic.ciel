// ciel-test: run
// expect-exit: 101
// expect-stderr-contains: slice range out of bounds
// expect-stderr-contains: slice_subview_panic.ciel

usize main() {
    []i64 values = [1, 2, 3];
    []i64 bad = values[2..1];
    return bad.len;
}
