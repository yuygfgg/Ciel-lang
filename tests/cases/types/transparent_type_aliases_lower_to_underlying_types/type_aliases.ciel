// ciel-test: run
// expect-exit: 4

type Count = i64;

struct Box<T> {
    T value;
}

type BoxOf<T> = Box<T>;

Count main() {
    BoxOf<Count> box = { value: 4 };
    Count value = box.value;
    return value;
}
