// ciel-test: error
// expect-error: field access on unsafe struct `RawFd` requires unsafe block

import /std/lib;
import /std/os/fd as fd;

i32 main() {
    fd::RawFd raw = fd::stdout();
    return raw.raw as i32;
}
