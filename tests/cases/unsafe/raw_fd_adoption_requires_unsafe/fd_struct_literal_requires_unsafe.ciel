// ciel-test: error
// expect-error: constructing unsafe struct `RawFd` requires unsafe block

import /std/lib;
import /std/os/fd as fd;

i32 main() {
    fd::RawFd raw = { raw: 1 as c_int };
    return unsafe { fd::raw_fd(raw) } as i32;
}
