// ciel-test: error
// expect-error: call to unsafe function `from_raw_fd` requires unsafe block

import /std/lib;
import /std/os/fd as fd;

i32 main() {
    fd::RawFd raw = fd::from_raw_fd(1 as c_int);
    return unsafe { fd::raw_fd(raw) } as i32;
}
