// ciel-test: error
// expect-error: call to unsafe function `abs` requires unsafe block

#c_include "stdlib.h"

unsafe extern "C" {
    i32 abs(i32 value);
}

unsafe i32 wrapper(i32 value) {
    return abs(value);
}

i32 main() {
    return unsafe { wrapper(-7) };
}
