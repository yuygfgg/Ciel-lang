// ciel-test: error
// expect-error: use of unsafe function `abs` as a value requires unsafe block

#c_include "stdlib.h"

unsafe extern "C" {
    i32 abs(i32 value);
}

i32 main() {
    extern "C" i32 fn(i32) f = abs;
    return f(-5);
}
