// ciel-test: error
// expect-error: `unsafe impl` cannot implement safe interface `format_value`

interface<T> []const char format_value(*const T value);

struct Event {
    i64 value;
}

unsafe impl format_value(*const Event value) {
    return "event";
}

i32 main() {
    return 0;
}
